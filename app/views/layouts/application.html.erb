<!DOCTYPE html>
<html>
  <head>
    <title>Final</title>
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>
  <body>

    <header class="navbar navbar-fixed-top navbar-inverse">
    <div class="navbar-inner">
      <div class="container">
        <%= link_to "Final Project", root_path, class: "brand" %>
        <ul class="nav pull-right">
          <li class="divider-vertical"></li>
          <li><%= link_to "Public Data", root_path %></li>
          <li class="divider-vertical"></li>

          <% if user_signed_in? %>
            <li><%= link_to "Private Data", root_path %></li>
            <li class="divider-vertical"></li>
          <% end %>

          <% if user_signed_in? %>
            <li><%= link_to "Sign Out", destroy_user_session_path %></li>
          <% else %>
            <li><%= link_to "Sign In", new_user_session_path %></li>
          <% end %>
          <li class="divider-vertical"></li>
        </ul>
      </div>
    </div>
    </header>

    <br />
    <br />
    <br />
    <br />
    <div class="container">
      <%= yield %>
    </div>

  </body>
</html>
